$(function() {

    var choice = $('#id_type')
    choice.on('change', function(){
        console.log(choice.val())
        if choice == 'BulkProduct')
        hide other product things
        show bulk product things


        call twice, once at beginning, once at end
                $('#id_type');
        })

})
